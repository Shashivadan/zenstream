import type { IEpisode } from '@/types'
import React from 'react'

export default function Episodes({ data }: { data: IEpisode[] }) {
  return (
    <div className="w-full rounded-md p-2 dark:bg-zinc-900/50 sm:p-3">
      episodes
    </div>
  );
}
